NAME="dash"
TAG="0.5.11"
SRC="https://git.kernel.org/pub/scm/utils/dash/dash.git"
_git_clone_tag
_chdir_build

./autogen.sh
./configure --without-libedit
make LDFLAGS="-static"
strip -s "src/${NAME}"
mv "src/${NAME}" "${_TARGET}/${NAME}.$(_git_head)"
